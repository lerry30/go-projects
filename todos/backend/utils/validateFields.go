package utils

/*
import (
	"reflect"
)

func IsFieldExists(genStruct any, name string) bool {
	strt := reflect.TypeOf(genStruct)
	for i := 0; i < strt.NumField(); i++ {
		field := strt.Field(i)
		if field.Name == name {
			return true
		}
	}
	return false
}
	*/